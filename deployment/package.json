{
  "name": "deployment",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install-browsers": "npx playwright install",
    "test": "npx playwright test",
    "test:ci": "CI=true npx playwright test"
  },
  "devDependencies": {
    "@oxctl/deployment-test-utils": "1.1.3",
    "@playwright/test": "~1.57.0",
    "dotenv": "^16.3.1"
  }
}
