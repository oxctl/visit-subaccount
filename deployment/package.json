{
  "name": "deployment",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install-browsers": "npx playwright install",
    "test": "npx playwright test",
    "test:ci": "CI=true npx playwright test"
  },
  "devDependencies": {
    "@oxctl/deployment-test-utils": "1.1.2",
    "@playwright/test": "^1.55.1",
    "dotenv": "^16.3.1"
  }
}
